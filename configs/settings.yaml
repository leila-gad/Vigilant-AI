signaling:
  host: "0.0.0.0"
  port: 8090        # ← port changé pour éviter le conflit

ai:
  weights: "models/yolov8n.pt"
  conf: 0.45
  device: "auto"
privacy:
  blur_kernel: 51
streaming:
  initial_bitrate: 2000
  min_bitrate: 500
  max_bitrate: 5000
  target_latency_ms: 150
ingestion:
  queue_size: 30
  buffer_size: 5